

## Re-add Contact Link to App Navigation

### Change

**`src/components/AppLayout.tsx`**

- Import `MessageSquare` from `lucide-react`
- Add `{ to: "/contact", icon: MessageSquare, label: "Contact" }` back to the `navItems` array

That's it -- one file, two lines.

