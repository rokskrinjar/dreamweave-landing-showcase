

## Make Calendar Date Heading Lighter

### Change

In `src/components/DreamCalendar.tsx` line 147, change the selected date heading from `text-foreground` to `text-muted-foreground` so it appears as a lighter grey instead of dark/black.

### File
- `src/components/DreamCalendar.tsx` — one class change on line 147

