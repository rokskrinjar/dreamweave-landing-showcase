

# Update Lifetime Dreamer Price from $199 to $99

A simple text change in the pricing section on the landing page.

## Change
Update the Lifetime Dreamer tier price from `$199` to `$99` in `src/components/CTASection.tsx` (line 42).

No backend or Stripe changes needed yet since payments aren't wired up.

